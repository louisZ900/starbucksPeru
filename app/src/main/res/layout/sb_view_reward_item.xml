<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="item"
            type="com.starbucks.peru.domain.response.SBPromotionsCarouselModel" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/sb_app_spacing_normal"
        android:layout_marginTop="@dimen/sb_app_spacing_small"
        android:layout_marginBottom="@dimen/sb_app_spacing_small">

        <androidx.cardview.widget.CardView
            android:id="@+id/cardView_image"
            android:layout_width="0dp"
            android:layout_height="190dp"
            app:cardCornerRadius="5dp"
            app:cardElevation="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <ImageView
                android:id="@+id/imageView_reward"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@null"
                app:imageFromUrl="@{item.fieldImage}"
                tools:src="@drawable/sb_app_bg_placeholder"
                android:scaleType="fitXY"/>
        </androidx.cardview.widget.CardView>

        <TextView
            style="@style/SbAppLabels_Avenir_TD_Medium_18"
            android:id="@+id/textView_description"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/sb_app_spacing_normal"
            android:text="@{item.title}"
            tools:text="Deja que Starbucks Rewards agregue un pequeño gusto a tu día."
            app:layout_constraintTop_toBottomOf="@id/cardView_image"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>